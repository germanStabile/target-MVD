import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 0,
    marginTop: 20,
    marginBottom: 20,
    alignItems: 'center'
  }
});

export default styles;
